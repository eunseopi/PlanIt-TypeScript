import styled from '@emotion/styled';

export const PostFormButtonWrapper = styled.div`
    position:fixed;
    display: flex;
    width: 100%;
    padding: 20px 20px 44px 20px;
    bottom: 0;
    background: var(--color-background-default-secondary);
    border-radius: 40px 40px 0 0;

    & > button {width: 100%};
`;